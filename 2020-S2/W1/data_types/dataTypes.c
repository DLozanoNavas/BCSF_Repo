#include <stdio.h>
#include <cs50.h>

// Estamos trabajando con la versión 99 de C.

int main(void){

    // Character: almacenar una sola letra
    char letra = '8'; // 1 byte - 8 bits

    // Integer: Almacena numeros naturales positivos o negativos:
    int entero = 1000; // 4 bytes - 32 bit

    // String (que no son específicos de C y vienen incluidos en cs50.h)
    string palabra = "Esto es un string!"; // Tienen un tamaño variable en memoria

    // Booleanos (que nos permiten elegir entre sí y no)
    // Pueden tener dos valores: Verdaderos (true) o Falsos (false)
    bool IceCreamTime = false; // 1 byte

    // Numeros de Punto Flotante: (ej. 1521.35)
    float PuntoFlotante = 1254874.231354; // 8 bytes

    // Doble Precisión
    double NumeroDeDoblePrecision = 1.1324654346846249546354;

    letra * entero;













}